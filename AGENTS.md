# Agent quick start

If `docs/local/agents.md` exists on this machine, read that first — it documents maintainer-specific database access.

Otherwise start at [docs/setup.md](docs/setup.md) for database and environment setup, then [docs/agents.md](docs/agents.md) for Python and test conventions.
